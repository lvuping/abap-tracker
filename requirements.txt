# SAP ABAP SY-UNAME 추적기 의존성 패키지
# Python 3.7+ 필요

# 기본 라이브러리 (Python 표준 라이브러리 사용)
# csv - CSV 파일 읽기/쓰기
# json - JSON 파일 처리  
# re - 정규식 패턴 매칭
# os - 파일 시스템 접근
# sys - 시스템 관련 기능

# 현재 버전에서는 외부 의존성이 없습니다.
# 모든 기능이 Python 표준 라이브러리로 구현되어 있습니다.

# 향후 추가 예정 패키지:
# pandas>=1.3.0          # 대용량 데이터 처리 (향후 버전)
# colorama>=0.4.4        # 컬러 터미널 출력 (향후 버전)  
# click>=8.0.0           # CLI 인터페이스 개선 (향후 버전)
# tqdm>=4.62.0           # 진행률 표시 (향후 버전)
